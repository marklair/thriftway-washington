!!!
%html{:lang => "en"}
  %head
    %meta{:charset => 'utf-8'}
    %title Webstop - Digital Marketing Solutions for Grocers
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}

    = stylesheet_link_tag    "application"
    = javascript_include_tag "application"
    /[if IE]
      = stylesheet_link_tag    "temp-ie"

    /= csrf_meta_tags

    = yield :styles
    / HTML5 shim adds support for HTML5 tags in IE6, IE7 & IE8
    /[if lt IE 9]
      %link{ :src => "http://fonts.googleapis.com/css?family=Open+Sans+Condensed:700|Open+Sans:400,300,600,700,800)"}
      %script{ :src => "http://html5shiv.googlecode.com/svn/trunk/html5.js"}

  %body#top{'data-spy' => 'scroll', 'data-target' => '.navbar'}
    = render :partial => "layouts/header"
    .container
      = render :partial => "layouts/messages"
      = yield
    .footer
      = yield :footer
    = yield :scripts


